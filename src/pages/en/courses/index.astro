---
import CoursesPage from "@/components/pages/CoursesPage.astro";
import { getCourseRouteData } from "@/courses/routes";

const locale = "en" as const;
const routes = await getCourseRouteData(locale);
---

<CoursesPage locale={locale} routes={routes} />
