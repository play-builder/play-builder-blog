---
import { getEntry } from "astro:content";
import AboutPage from "@/components/pages/AboutPage.astro";

const about = await getEntry("pages", "about-ko");
if (!about)
  throw new Error("Missing content entry: src/content/pages/about-ko.md");
---

<AboutPage locale="ko" {about} />
